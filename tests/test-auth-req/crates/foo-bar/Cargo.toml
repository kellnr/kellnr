[package]
authors = ["secana <stefan.hausotte@gmx.de>"]
description = "Test hyphens in crate names, now with more dependencies!"
edition = "2021"
name = "foo-bar"
publish = ["kellnr-test"]
version = "1.0.0"

[dependencies]
test_lib = {version = "2.0", registry = "kellnr-test"}

[workspace]
