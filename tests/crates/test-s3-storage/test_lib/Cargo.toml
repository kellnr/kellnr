[package]
authors = ["secana <stefan.hausotte@gmx.de>"]
description = "Test library with docs"
edition = "2021"
name = "test_lib"
publish = ["kellnr-test"]
version = "1.0.0"

[dependencies]
regex = "1.4.1"
serde = {version = "1.0", features = ["derive"]}

[workspace]
